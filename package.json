{
  "name": "furniture-platform",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "dev:api": "pnpm --filter @furniture/api dev",
    "dev:web": "pnpm --filter @furniture/web dev"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
